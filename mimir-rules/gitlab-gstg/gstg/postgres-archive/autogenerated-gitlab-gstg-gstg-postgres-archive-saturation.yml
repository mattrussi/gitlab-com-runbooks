# WARNING. DO NOT EDIT THIS FILE BY HAND. USE ./mimir-rules-jsonnet/saturation.jsonnet TO GENERATE IT
# YOUR CHANGES WILL BE OVERRIDDEN
groups:
- name: Saturation Rules (autogenerated)
  interval: 1m
  rules:
  - record: gitlab_component_saturation:ratio
    labels:
      component: cpu
    expr: |
      max by(env, environment, tier, type, stage, shard) (
        clamp_min(
          clamp_max(
            1 - avg by (env, environment, tier, type, stage, shard) (
              rate(node_cpu_seconds_total{mode="idle", env="gstg",type="postgres-archive"}[5m])
            )
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: disk_inodes
    expr: |
      max by(env, environment, tier, type, stage, shard, fqdn, device) (
        clamp_min(
          clamp_max(
            1 - (
              node_filesystem_files_free{fstype=~"(ext.|xfs)", env="gstg",type="postgres-archive"}
              /
              node_filesystem_files{fstype=~"(ext.|xfs)", env="gstg",type="postgres-archive"}
            )
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: disk_space
    expr: |
      max by(env, environment, tier, type, stage, shard, fqdn, device) (
        clamp_min(
          clamp_max(
            (
              1 - node_filesystem_avail_bytes{fstype=~"ext.|xfs", env="gstg",type="postgres-archive"} / node_filesystem_size_bytes{fstype=~"ext.|xfs", env="gstg",type="postgres-archive"}
            )
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: memory
    expr: |
      max by(env, environment, tier, type, stage, shard, fqdn) (
        clamp_min(
          clamp_max(
            instance:node_memory_utilization:ratio{env="gstg",type="postgres-archive"} or instance:node_memory_utilisation:ratio{env="gstg",type="postgres-archive"}
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: nf_conntrack_entries
    expr: |
      max by(env, environment, tier, type, stage, shard, fqdn, instance) (
        clamp_min(
          clamp_max(
            max_over_time(node_nf_conntrack_entries{env="gstg",type="postgres-archive"}[1m])
            /
            node_nf_conntrack_entries_limit{env="gstg",type="postgres-archive"}
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: node_schedstat_waiting
    expr: |
      max by(env, environment, tier, type, stage, shard, fqdn, shard) (
        clamp_min(
          clamp_max(
            avg without (cpu) (rate(node_schedstat_waiting_seconds_total{env="gstg",type="postgres-archive"}[1h]))
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: open_fds
    expr: |
      max by(env, environment, tier, type, stage, shard, job, instance) (
        clamp_min(
          clamp_max(
            (
              process_open_fds{env="gstg",type="postgres-archive"}
              /
              process_max_fds{env="gstg",type="postgres-archive"}
            )
            or
            (
              ruby_file_descriptors{env="gstg",type="postgres-archive"}
              /
              ruby_process_max_fds{env="gstg",type="postgres-archive"}
            )
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: pg_active_db_connections_replica
    expr: |
      max by(env, environment, tier, type, stage, shard, fqdn) (
        clamp_min(
          clamp_max(
            sum without (state) (
              pg_stat_activity_count{datname=~"gitlabhq_production|gitlabhq_registry", state!="idle", env="gstg",type="postgres-archive"} and on(instance) (pg_replication_is_replica == 1)
            )
            / on (env, environment, tier, type, stage, shard, fqdn)
            pg_settings_max_connections{env="gstg",type="postgres-archive"}
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: pg_btree_bloat
    expr: |
      max by(env, environment, tier, type, stage, shard, fqdn) (
        clamp_min(
          clamp_max(
            sum by (env, environment, tier, type, stage, shard, fqdn) (last_over_time(gitlab_database_bloat_btree_bloat_size{env="gstg",type="postgres-archive"}[1h]))
            /
            sum by (env, environment, tier, type, stage, shard, fqdn) (last_over_time(gitlab_database_bloat_btree_real_size{env="gstg",type="postgres-archive"}[1h]))
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: pg_primary_cpu
    expr: |
      max by(env, environment, tier, type, stage, shard, fqdn) (
        clamp_min(
          clamp_max(
            avg without(cpu, mode) (
              1
              -
              (
                rate(node_cpu_seconds_total{mode="idle", env="gstg",type="postgres-archive"}[5m])
                and on(fqdn)
                pg_replication_is_replica{env="gstg",type="postgres-archive"} == 0
              )
            )
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: pg_table_bloat
    expr: |
      max by(env, environment, tier, type, stage, shard, fqdn) (
        clamp_min(
          clamp_max(
            sum by (env, environment, tier, type, stage, shard, fqdn) (avg_over_time(gitlab_database_bloat_table_bloat_size{env="gstg",type="postgres-archive"}[58m]))
            /
            sum by (env, environment, tier, type, stage, shard, fqdn) (avg_over_time(gitlab_database_bloat_table_real_size{env="gstg",type="postgres-archive"}[58m]))
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: shard_cpu
    expr: |
      max by(env, environment, tier, type, stage, shard, shard) (
        clamp_min(
          clamp_max(
            1 - avg by (env, environment, tier, type, stage, shard, shard) (
              rate(node_cpu_seconds_total{mode="idle", env="gstg",type="postgres-archive"}[5m])
            )
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: single_node_cpu
    expr: |
      max by(env, environment, tier, type, stage, shard, fqdn) (
        clamp_min(
          clamp_max(
            avg without(cpu, mode) (1 - rate(node_cpu_seconds_total{mode="idle", env="gstg",type="postgres-archive"}[5m]))
            ,
            1)
        ,
        0)
      )
