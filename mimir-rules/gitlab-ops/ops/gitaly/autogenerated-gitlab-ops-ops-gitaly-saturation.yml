# WARNING. DO NOT EDIT THIS FILE BY HAND. USE ./mimir-rules-jsonnet/saturation.jsonnet TO GENERATE IT
# YOUR CHANGES WILL BE OVERRIDDEN
groups:
- name: Saturation Rules (autogenerated)
  interval: 1m
  rules:
  - record: gitlab_component_saturation:ratio
    labels:
      component: cgroup_memory
    expr: |
      max by(env, environment, tier, type, stage, shard) (
        clamp_min(
          clamp_max(
            (
              container_memory_usage_bytes{id="/system.slice/gitlab-runsvdir.service", env="ops",type="gitaly"} -
              container_memory_cache{id="/system.slice/gitlab-runsvdir.service", env="ops",type="gitaly"} -
              container_memory_swap{id="/system.slice/gitlab-runsvdir.service", env="ops",type="gitaly"}
            )
            /
            container_spec_memory_limit_bytes{id="/system.slice/gitlab-runsvdir.service", env="ops",type="gitaly"}
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: cpu
    expr: |
      max by(env, environment, tier, type, stage, shard) (
        clamp_min(
          clamp_max(
            1 - avg by (env, environment, tier, type, stage, shard) (
              rate(node_cpu_seconds_total{mode="idle", env="ops",type="gitaly"}[5m])
            )
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: disk_inodes
    expr: |
      max by(env, environment, tier, type, stage, shard) (
        clamp_min(
          clamp_max(
            1 - (
              node_filesystem_files_free{fstype=~"(ext.|xfs)", env="ops",type="gitaly"}
              /
              node_filesystem_files{fstype=~"(ext.|xfs)", env="ops",type="gitaly"}
            )
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: disk_space
    expr: |
      max by(env, environment, tier, type, stage, shard) (
        clamp_min(
          clamp_max(
            (
              1 - node_filesystem_avail_bytes{fstype=~"ext.|xfs", env="ops",type="gitaly"} / node_filesystem_size_bytes{fstype=~"ext.|xfs", env="ops",type="gitaly"}
            )
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: disk_sustained_read_iops
    expr: |
      max by(env, environment, tier, type, stage, shard) (
        clamp_min(
          clamp_max(
            rate(node_disk_reads_completed_total{device!="sda", env="ops",type="gitaly"}[20m])
            /
            node_disk_max_read_iops{device!="sda", env="ops",type="gitaly"}
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: disk_sustained_read_throughput
    expr: |
      max by(env, environment, tier, type, stage, shard) (
        clamp_min(
          clamp_max(
            rate(node_disk_read_bytes_total{device!="sda", env="ops",type="gitaly"}[20m])
            /
            node_disk_max_read_bytes_seconds{device!="sda", env="ops",type="gitaly"}
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: disk_sustained_write_iops
    expr: |
      max by(env, environment, tier, type, stage, shard) (
        clamp_min(
          clamp_max(
            rate(node_disk_writes_completed_total{device!="sda", env="ops",type="gitaly"}[20m])
            /
            node_disk_max_write_iops{device!="sda", env="ops",type="gitaly"}
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: disk_sustained_write_throughput
    expr: |
      max by(env, environment, tier, type, stage, shard) (
        clamp_min(
          clamp_max(
            rate(node_disk_written_bytes_total{device!="sda", env="ops",type="gitaly"}[20m])
            /
            node_disk_max_write_bytes_seconds{device!="sda", env="ops",type="gitaly"}
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: gitaly_active_node_available_space
    expr: |
      max by(env, environment, tier, type, stage, shard) (
        clamp_min(
          clamp_max(
            1 - (
              sum by (env, environment, tier, type, stage, shard, shard) (
                (
                  node_filesystem_avail_bytes{env="ops",type="gitaly", fqdn=~"gitaly-.*-stor-.*",mountpoint="/var/opt/gitlab",shard=~"default|praefect"} -
                  (node_filesystem_size_bytes{env="ops",type="gitaly", fqdn=~"gitaly-.*-stor-.*",mountpoint="/var/opt/gitlab",shard=~"default|praefect"} * 0.25)
                )
                and
                (instance:node_filesystem_avail:ratio{env="ops",type="gitaly", fqdn=~"gitaly-.*-stor-.*",mountpoint="/var/opt/gitlab",shard=~"default|praefect"} > 0.25)
              )
              /
              sum by (env, environment, tier, type, stage, shard, shard)(
                node_filesystem_size_bytes{env="ops",type="gitaly", fqdn=~"gitaly-.*-stor-.*",mountpoint="/var/opt/gitlab",shard=~"default|praefect"}
              )
            )
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: gitaly_total_disk_space
    expr: |
      max by(env, environment, tier, type, stage, shard) (
        clamp_min(
          clamp_max(
            1 - (
              sum by (env, environment, tier, type, stage, shard, shard) (
                node_filesystem_avail_bytes{env="ops",type="gitaly", mountpoint="/var/opt/gitlab"}
              )
              /
              sum by (env, environment, tier, type, stage, shard, shard) (
                node_filesystem_size_bytes{env="ops",type="gitaly", mountpoint="/var/opt/gitlab"}
              )
            )
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: go_goroutines
    expr: |
      max by(env, environment, tier, type, stage, shard) (
        clamp_min(
          clamp_max(
            sum by (env, environment, tier, type, stage, shard, fqdn) (
              go_goroutines{env="ops",type="gitaly"}
            )
            /
            250000
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: go_memory
    expr: |
      max by(env, environment, tier, type, stage, shard) (
        clamp_min(
          clamp_max(
            max by (env, environment, tier, type, stage, shard, fqdn) (
              go_memstats_alloc_bytes{env="ops",type="gitaly"}
            )
            /
            sum by (env, environment, tier, type, stage, shard, fqdn) (
              node_memory_MemTotal_bytes{env="ops",type="gitaly"}
            )
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: memory
    expr: |
      max by(env, environment, tier, type, stage, shard) (
        clamp_min(
          clamp_max(
            instance:node_memory_utilization:ratio{env="ops",type="gitaly"} or instance:node_memory_utilisation:ratio{env="ops",type="gitaly"}
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: nf_conntrack_entries
    expr: |
      max by(env, environment, tier, type, stage, shard) (
        clamp_min(
          clamp_max(
            max_over_time(node_nf_conntrack_entries{env="ops",type="gitaly"}[1m])
            /
            node_nf_conntrack_entries_limit{env="ops",type="gitaly"}
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: node_schedstat_waiting
    expr: |
      max by(env, environment, tier, type, stage, shard) (
        clamp_min(
          clamp_max(
            avg without (cpu) (rate(node_schedstat_waiting_seconds_total{env="ops",type="gitaly"}[1h]))
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: open_fds
    expr: |
      max by(env, environment, tier, type, stage, shard) (
        clamp_min(
          clamp_max(
            (
              process_open_fds{env="ops",type="gitaly"}
              /
              process_max_fds{env="ops",type="gitaly"}
            )
            or
            (
              ruby_file_descriptors{env="ops",type="gitaly"}
              /
              ruby_process_max_fds{env="ops",type="gitaly"}
            )
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: shard_cpu
    expr: |
      max by(env, environment, tier, type, stage, shard) (
        clamp_min(
          clamp_max(
            1 - avg by (env, environment, tier, type, stage, shard, shard) (
              rate(node_cpu_seconds_total{mode="idle", env="ops",type="gitaly"}[5m])
            )
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: single_node_cpu
    expr: |
      max by(env, environment, tier, type, stage, shard) (
        clamp_min(
          clamp_max(
            avg without(cpu, mode) (1 - rate(node_cpu_seconds_total{mode="idle", env="ops",type="gitaly"}[5m]))
            ,
            1)
        ,
        0)
      )
