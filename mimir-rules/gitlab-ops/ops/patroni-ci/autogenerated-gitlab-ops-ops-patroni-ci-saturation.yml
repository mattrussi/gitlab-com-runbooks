# WARNING. DO NOT EDIT THIS FILE BY HAND. USE ./mimir-rules-jsonnet/saturation.jsonnet TO GENERATE IT
# YOUR CHANGES WILL BE OVERRIDDEN
groups:
- name: Saturation Rules (autogenerated)
  interval: 1m
  rules:
  - record: gitlab_component_saturation:ratio
    labels:
      component: cpu
    expr: |
      max by(env, environment, tier, type, stage, shard) (
        clamp_min(
          clamp_max(
            1 - avg by (env, environment, tier, type, stage, shard) (
              rate(node_cpu_seconds_total{mode="idle", env="ops",type="patroni-ci"}[5m])
            )
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: disk_inodes
    expr: |
      max by(env, environment, tier, type, stage, shard) (
        clamp_min(
          clamp_max(
            1 - (
              node_filesystem_files_free{fstype=~"(ext.|xfs)", env="ops",type="patroni-ci"}
              /
              node_filesystem_files{fstype=~"(ext.|xfs)", env="ops",type="patroni-ci"}
            )
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: disk_space
    expr: |
      max by(env, environment, tier, type, stage, shard) (
        clamp_min(
          clamp_max(
            (
              1 - node_filesystem_avail_bytes{fstype=~"ext.|xfs", env="ops",type="patroni-ci"} / node_filesystem_size_bytes{fstype=~"ext.|xfs", env="ops",type="patroni-ci"}
            )
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: disk_sustained_read_iops
    expr: |
      max by(env, environment, tier, type, stage, shard) (
        clamp_min(
          clamp_max(
            rate(node_disk_reads_completed_total{device!="sda", env="ops",type="patroni-ci"}[20m])
            /
            node_disk_max_read_iops{device!="sda", env="ops",type="patroni-ci"}
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: disk_sustained_read_throughput
    expr: |
      max by(env, environment, tier, type, stage, shard) (
        clamp_min(
          clamp_max(
            rate(node_disk_read_bytes_total{device!="sda", env="ops",type="patroni-ci"}[20m])
            /
            node_disk_max_read_bytes_seconds{device!="sda", env="ops",type="patroni-ci"}
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: disk_sustained_write_iops
    expr: |
      max by(env, environment, tier, type, stage, shard) (
        clamp_min(
          clamp_max(
            rate(node_disk_writes_completed_total{device!="sda", env="ops",type="patroni-ci"}[20m])
            /
            node_disk_max_write_iops{device!="sda", env="ops",type="patroni-ci"}
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: disk_sustained_write_throughput
    expr: |
      max by(env, environment, tier, type, stage, shard) (
        clamp_min(
          clamp_max(
            rate(node_disk_written_bytes_total{device!="sda", env="ops",type="patroni-ci"}[20m])
            /
            node_disk_max_write_bytes_seconds{device!="sda", env="ops",type="patroni-ci"}
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: memory
    expr: |
      max by(env, environment, tier, type, stage, shard) (
        clamp_min(
          clamp_max(
            instance:node_memory_utilization:ratio{env="ops",type="patroni-ci"} or instance:node_memory_utilisation:ratio{env="ops",type="patroni-ci"}
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: nf_conntrack_entries
    expr: |
      max by(env, environment, tier, type, stage, shard) (
        clamp_min(
          clamp_max(
            max_over_time(node_nf_conntrack_entries{env="ops",type="patroni-ci"}[1m])
            /
            node_nf_conntrack_entries_limit{env="ops",type="patroni-ci"}
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: node_schedstat_waiting
    expr: |
      max by(env, environment, tier, type, stage, shard) (
        clamp_min(
          clamp_max(
            avg without (cpu) (rate(node_schedstat_waiting_seconds_total{env="ops",type="patroni-ci"}[1h]))
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: open_fds
    expr: |
      max by(env, environment, tier, type, stage, shard) (
        clamp_min(
          clamp_max(
            (
              process_open_fds{env="ops",type="patroni-ci"}
              /
              process_max_fds{env="ops",type="patroni-ci"}
            )
            or
            (
              ruby_file_descriptors{env="ops",type="patroni-ci"}
              /
              ruby_process_max_fds{env="ops",type="patroni-ci"}
            )
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: pg_active_db_connections_primary
    expr: |
      max by(env, environment, tier, type, stage, shard) (
        clamp_min(
          clamp_max(
            sum without (state) (
              pg_stat_activity_count{datname=~"gitlabhq_production|gitlabhq_registry", state!="idle", env="ops",type="patroni-ci"} unless on(instance) (pg_replication_is_replica == 1)
            )
            / on (env, environment, tier, type, stage, shard, fqdn)
            pg_settings_max_connections{env="ops",type="patroni-ci"}
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: pg_active_db_connections_replica
    expr: |
      max by(env, environment, tier, type, stage, shard) (
        clamp_min(
          clamp_max(
            sum without (state) (
              pg_stat_activity_count{datname=~"gitlabhq_production|gitlabhq_registry", state!="idle", env="ops",type="patroni-ci"} and on(instance) (pg_replication_is_replica == 1)
            )
            / on (env, environment, tier, type, stage, shard, fqdn)
            pg_settings_max_connections{env="ops",type="patroni-ci"}
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: pg_btree_bloat
    expr: |
      max by(env, environment, tier, type, stage, shard) (
        clamp_min(
          clamp_max(
            sum by (env, environment, tier, type, stage, shard, fqdn) (last_over_time(gitlab_database_bloat_btree_bloat_size{env="ops",type="patroni-ci"}[1h]))
            /
            sum by (env, environment, tier, type, stage, shard, fqdn) (last_over_time(gitlab_database_bloat_btree_real_size{env="ops",type="patroni-ci"}[1h]))
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: pg_int4_id
    expr: |
      max by(env, environment, tier, type, stage, shard) (
        clamp_min(
          clamp_max(
            max by (env, environment, tier, type, stage, shard, column_name) (
              pg_int4_saturation_current_largest_value{env="ops",type="patroni-ci",column_name!~".+(.|-|_)iid"} / pg_int4_saturation_column_max_value{env="ops",type="patroni-ci",column_name!~".+(.|-|_)iid"}
            )
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: pg_primary_cpu
    expr: |
      max by(env, environment, tier, type, stage, shard) (
        clamp_min(
          clamp_max(
            avg without(cpu, mode) (
              1
              -
              (
                rate(node_cpu_seconds_total{mode="idle", env="ops",type="patroni-ci"}[5m])
                and on(fqdn)
                pg_replication_is_replica{env="ops",type="patroni-ci"} == 0
              )
            )
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: pg_table_bloat
    expr: |
      max by(env, environment, tier, type, stage, shard) (
        clamp_min(
          clamp_max(
            sum by (env, environment, tier, type, stage, shard, fqdn) (avg_over_time(gitlab_database_bloat_table_bloat_size{env="ops",type="patroni-ci"}[58m]))
            /
            sum by (env, environment, tier, type, stage, shard, fqdn) (avg_over_time(gitlab_database_bloat_table_real_size{env="ops",type="patroni-ci"}[58m]))
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: pg_txid_vacuum_to_wraparound
    expr: |
      max by(env, environment, tier, type, stage, shard) (
        clamp_min(
          clamp_max(
            (
              sum by (env, environment, tier, type, stage, shard) (
                increase(fluentd_pg_auto_vacuum_elapsed_seconds_total{env="ops",type="patroni-ci"}[1d])
              )
              / on(environment, type) group_left() (
                pg_settings_autovacuum_max_workers and
                on (instance) pg_replication_is_replica == 0
              )
            )
            /
            (
              (2^31 - 10^6)
              /
              (
                avg by (env, environment, tier, type, stage, shard) (
                  deriv(pg_txid_current{env="ops",type="patroni-ci"}[1d]) and
                  on (instance) pg_replication_is_replica == 0
                )
              )
            )
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: pg_vacuum_activity_v2
    expr: |
      max by(env, environment, tier, type, stage, shard) (
        clamp_min(
          clamp_max(
            sum by (env, environment, tier, type, stage, shard) (
              rate(fluentd_pg_auto_vacuum_elapsed_seconds_total{env="ops",type="patroni-ci"}[1d])
              and on (fqdn) (pg_replication_is_replica{env="ops",type="patroni-ci"} == 0)
            )
            /
            avg by (env, environment, tier, type, stage, shard) (
              pg_settings_autovacuum_max_workers{env="ops",type="patroni-ci"}
              and on (instance, job) (pg_replication_is_replica{env="ops",type="patroni-ci"} == 0)
            )
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: pg_walsender_cpu
    expr: |
      max by(env, environment, tier, type, stage, shard) (
        clamp_min(
          clamp_max(
            sum by (env, environment, tier, type, stage, shard) (
              sum by(env, environment, tier, type, stage, shard, fqdn) (
                rate(namedprocess_namegroup_cpu_seconds_total{env="ops",type="patroni-ci", groupname=~"pg.worker.walsender|pg.worker.walwriter|wal-g"}[5m])
                and on (fqdn) (pg_replication_is_replica{env="ops",type="patroni-ci"} == 0)
              )
              /
              count by (env, environment, tier, type, stage, shard, fqdn) (
                node_cpu_seconds_total{env="ops",type="patroni-ci", mode="idle"} and on(fqdn) (pg_replication_is_replica{env="ops",type="patroni-ci"} == 0)
              )
            )
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: pg_xid_wraparound
    expr: |
      max by(env, environment, tier, type, stage, shard) (
        clamp_min(
          clamp_max(
            (
              max without (series) (
                label_replace(pg_database_wraparound_age_datfrozenxid{env="ops",type="patroni-ci"}, "series", "datfrozenxid", "", "")
                or
                label_replace(pg_database_wraparound_age_datminmxid{env="ops",type="patroni-ci"}, "series", "datminmxid", "", "")
              )
              and on (instance, job) (pg_replication_is_replica{env="ops",type="patroni-ci"} == 0)
            )
            /
            (2^31 - 3000000)
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: pgbouncer_async_replica_pool
    expr: |
      max by(env, environment, tier, type, stage, shard) (
        clamp_min(
          clamp_max(
            (
              avg_over_time(pgbouncer_pools_server_active_connections{user="gitlab", database="gitlabhq_production_sidekiq", env="ops",type="patroni-ci"}[5m]) +
              avg_over_time(pgbouncer_pools_server_testing_connections{user="gitlab", database="gitlabhq_production_sidekiq", env="ops",type="patroni-ci"}[5m]) +
              avg_over_time(pgbouncer_pools_server_used_connections{user="gitlab", database="gitlabhq_production_sidekiq", env="ops",type="patroni-ci"}[5m]) +
              avg_over_time(pgbouncer_pools_server_login_connections{user="gitlab", database="gitlabhq_production_sidekiq", env="ops",type="patroni-ci"}[5m])
            )
            / on(env, environment, tier, type, stage, shard, fqdn, instance) group_left()
            sum by (env, environment, tier, type, stage, shard, fqdn, instance) (
              avg_over_time(pgbouncer_databases_pool_size{name="gitlabhq_production_sidekiq", env="ops",type="patroni-ci"}[5m])
            )
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: pgbouncer_client_conn_replicas
    expr: |
      max by(env, environment, tier, type, stage, shard) (
        clamp_min(
          clamp_max(
            max_over_time(pgbouncer_used_clients{env="ops",type="patroni-ci"}[5m])
            /
            30000
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: pgbouncer_sync_replica_pool
    expr: |
      max by(env, environment, tier, type, stage, shard) (
        clamp_min(
          clamp_max(
            (
              avg_over_time(pgbouncer_pools_server_active_connections{user=~"gitlab|gitlab-registry", database=~"gitlabhq_registry|gitlabhq_production", env="ops",type="patroni-ci"}[5m]) +
              avg_over_time(pgbouncer_pools_server_testing_connections{user=~"gitlab|gitlab-registry", database=~"gitlabhq_registry|gitlabhq_production", env="ops",type="patroni-ci"}[5m]) +
              avg_over_time(pgbouncer_pools_server_used_connections{user=~"gitlab|gitlab-registry", database=~"gitlabhq_registry|gitlabhq_production", env="ops",type="patroni-ci"}[5m]) +
              avg_over_time(pgbouncer_pools_server_login_connections{user=~"gitlab|gitlab-registry", database=~"gitlabhq_registry|gitlabhq_production", env="ops",type="patroni-ci"}[5m])
            )
            / on(env, environment, tier, type, stage, shard, fqdn, instance) group_left()
            sum by (env, environment, tier, type, stage, shard, fqdn, instance) (
              avg_over_time(pgbouncer_databases_pool_size{name=~"gitlabhq_registry|gitlabhq_production", env="ops",type="patroni-ci"}[5m])
            )
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: shard_cpu
    expr: |
      max by(env, environment, tier, type, stage, shard) (
        clamp_min(
          clamp_max(
            1 - avg by (env, environment, tier, type, stage, shard, shard) (
              rate(node_cpu_seconds_total{mode="idle", env="ops",type="patroni-ci"}[5m])
            )
            ,
            1)
        ,
        0)
      )
  - record: gitlab_component_saturation:ratio
    labels:
      component: single_node_cpu
    expr: |
      max by(env, environment, tier, type, stage, shard) (
        clamp_min(
          clamp_max(
            avg without(cpu, mode) (1 - rate(node_cpu_seconds_total{mode="idle", env="ops",type="patroni-ci"}[5m]))
            ,
            1)
        ,
        0)
      )
