# WARNING. DO NOT EDIT THIS FILE BY HAND. USE ./thanos-rules-jsonnet/service-component-alerts.jsonnet TO GENERATE IT
# YOUR CHANGES WILL BE OVERRIDDEN
groups:
- name: 'Service Component Alerts: internal-api'
  interval: 1m
  partial_response_strategy: warn
  rules:
  - alert: InternalApiServiceRailsRequestApdexSLOViolation
    for: 2m
    annotations:
      title: The rails_request SLI of the internal-api service (`{{ $labels.stage
        }}` stage) has an apdex violating SLO
      description: |
        The number of requests meeting their duration target based on the urgency of the endpoint. By default, a request should take no more than 1s. But this can be adjusted by endpoint.

        Read more about this in the [documentation](https://docs.gitlab.com/ee/development/application_slis/rails_request_apdex.html).

        Currently the apdex value is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: internal-api-main/internal-api-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/internal-api-main/internal-api-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "495984175"
      grafana_variables: environment,stage
      runbook: docs/internal-api/README.md
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: in_source_metrics
      rules_domain: general
      severity: s3
      sli_type: apdex
      slo_alert: "yes"
      user_impacting: "yes"
      window: 1h
    expr: |
      (
        (
          gitlab_component_apdex:ratio_1h{component="rails_request",monitor="global",type="internal-api"}
          < (1 - 14.4 * 0.010000)
        )
        and
        (
          gitlab_component_apdex:ratio_5m{component="rails_request",monitor="global",type="internal-api"}
          < (1 - 14.4 * 0.010000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_1h{component="rails_request",monitor="global",type="internal-api"}) >= 1
      )
  - alert: InternalApiServiceRailsRequestApdexSLOViolation
    for: 2m
    annotations:
      title: The rails_request SLI of the internal-api service (`{{ $labels.stage
        }}` stage) has an apdex violating SLO
      description: |
        The number of requests meeting their duration target based on the urgency of the endpoint. By default, a request should take no more than 1s. But this can be adjusted by endpoint.

        Read more about this in the [documentation](https://docs.gitlab.com/ee/development/application_slis/rails_request_apdex.html).

        Currently the apdex value is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: internal-api-main/internal-api-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/internal-api-main/internal-api-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "495984175"
      grafana_variables: environment,stage
      runbook: docs/internal-api/README.md
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: in_source_metrics
      rules_domain: general
      severity: s3
      sli_type: apdex
      slo_alert: "yes"
      user_impacting: "yes"
      window: 6h
    expr: |
      (
        (
          gitlab_component_apdex:ratio_6h{component="rails_request",monitor="global",type="internal-api"}
          < (1 - 6 * 0.010000)
        )
        and
        (
          gitlab_component_apdex:ratio_30m{component="rails_request",monitor="global",type="internal-api"}
          < (1 - 6 * 0.010000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_6h{component="rails_request",monitor="global",type="internal-api"}) >= 0.16667
      )
  - alert: InternalApiServiceRailsRequestApdexSLOViolationRegional
    for: 2m
    annotations:
      title: The rails_request SLI of the internal-api service in region `{{ $labels.region
        }}` has an apdex violating SLO
      description: |
        The number of requests meeting their duration target based on the urgency of the endpoint. By default, a request should take no more than 1s. But this can be adjusted by endpoint.

        Read more about this in the [documentation](https://docs.gitlab.com/ee/development/application_slis/rails_request_apdex.html).

        Note that this alert is specific to the `{{ $labels.region }}` region.

        Currently the apdex value is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: internal-api-main/internal-api-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/internal-api-main/internal-api-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "495984175"
      grafana_variables: environment,stage
      runbook: docs/internal-api/README.md
    labels:
      aggregation: regional_component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: in_source_metrics
      rules_domain: general
      severity: s3
      sli_type: apdex
      slo_alert: "yes"
      user_impacting: "yes"
      window: 1h
    expr: |
      (
        (
          gitlab_regional_sli_apdex:ratio_1h{component="rails_request",monitor="global",type="internal-api"}
          < (1 - 14.4 * 0.010000)
        )
        and
        (
          gitlab_regional_sli_apdex:ratio_5m{component="rails_request",monitor="global",type="internal-api"}
          < (1 - 14.4 * 0.010000)
        )
      )
      and on(env,environment,tier,type,stage,region,component)
      (
        sum by(env,environment,tier,type,stage,region,component) (gitlab_regional_sli_ops:rate_1h{component="rails_request",monitor="global",type="internal-api"}) >= 1
      )
  - alert: InternalApiServiceRailsRequestApdexSLOViolationRegional
    for: 2m
    annotations:
      title: The rails_request SLI of the internal-api service in region `{{ $labels.region
        }}` has an apdex violating SLO
      description: |
        The number of requests meeting their duration target based on the urgency of the endpoint. By default, a request should take no more than 1s. But this can be adjusted by endpoint.

        Read more about this in the [documentation](https://docs.gitlab.com/ee/development/application_slis/rails_request_apdex.html).

        Note that this alert is specific to the `{{ $labels.region }}` region.

        Currently the apdex value is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: internal-api-main/internal-api-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/internal-api-main/internal-api-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "495984175"
      grafana_variables: environment,stage
      runbook: docs/internal-api/README.md
    labels:
      aggregation: regional_component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: in_source_metrics
      rules_domain: general
      severity: s3
      sli_type: apdex
      slo_alert: "yes"
      user_impacting: "yes"
      window: 6h
    expr: |
      (
        (
          gitlab_regional_sli_apdex:ratio_6h{component="rails_request",monitor="global",type="internal-api"}
          < (1 - 6 * 0.010000)
        )
        and
        (
          gitlab_regional_sli_apdex:ratio_30m{component="rails_request",monitor="global",type="internal-api"}
          < (1 - 6 * 0.010000)
        )
      )
      and on(env,environment,tier,type,stage,region,component)
      (
        sum by(env,environment,tier,type,stage,region,component) (gitlab_regional_sli_ops:rate_6h{component="rails_request",monitor="global",type="internal-api"}) >= 0.16667
      )
  - alert: InternalApiServiceRailsRequestErrorSLOViolation
    for: 2m
    annotations:
      title: The rails_request SLI of the internal-api service (`{{ $labels.stage
        }}` stage) has an error rate violating SLO
      description: |
        The number of requests meeting their duration target based on the urgency of the endpoint. By default, a request should take no more than 1s. But this can be adjusted by endpoint.

        Read more about this in the [documentation](https://docs.gitlab.com/ee/development/application_slis/rails_request_apdex.html).

        Currently the error-rate is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: internal-api-main/internal-api-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/internal-api-main/internal-api-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "387946920"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          sli_aggregations:gitlab_sli_rails_request_error_total_rate5m{environment="{{ $labels.environment }}",job="gitlab-rails",stage="{{ $labels.stage }}",type="internal-api"}
        )
      runbook: docs/internal-api/README.md
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: in_source_metrics
      rules_domain: general
      severity: s3
      sli_type: error
      slo_alert: "yes"
      user_impacting: "yes"
      window: 1h
    expr: |
      (
        (
          gitlab_component_errors:ratio_1h{component="rails_request",monitor="global",type="internal-api"}
          > (14.4 * 0.001000)
        )
        and
        (
          gitlab_component_errors:ratio_5m{component="rails_request",monitor="global",type="internal-api"}
          > (14.4 * 0.001000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_1h{component="rails_request",monitor="global",type="internal-api"}) >= 1
      )
  - alert: InternalApiServiceRailsRequestErrorSLOViolation
    for: 2m
    annotations:
      title: The rails_request SLI of the internal-api service (`{{ $labels.stage
        }}` stage) has an error rate violating SLO
      description: |
        The number of requests meeting their duration target based on the urgency of the endpoint. By default, a request should take no more than 1s. But this can be adjusted by endpoint.

        Read more about this in the [documentation](https://docs.gitlab.com/ee/development/application_slis/rails_request_apdex.html).

        Currently the error-rate is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: internal-api-main/internal-api-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/internal-api-main/internal-api-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "387946920"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          sli_aggregations:gitlab_sli_rails_request_error_total_rate5m{environment="{{ $labels.environment }}",job="gitlab-rails",stage="{{ $labels.stage }}",type="internal-api"}
        )
      runbook: docs/internal-api/README.md
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: in_source_metrics
      rules_domain: general
      severity: s3
      sli_type: error
      slo_alert: "yes"
      user_impacting: "yes"
      window: 6h
    expr: |
      (
        (
          gitlab_component_errors:ratio_6h{component="rails_request",monitor="global",type="internal-api"}
          > (6 * 0.001000)
        )
        and
        (
          gitlab_component_errors:ratio_30m{component="rails_request",monitor="global",type="internal-api"}
          > (6 * 0.001000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_6h{component="rails_request",monitor="global",type="internal-api"}) >= 0.16667
      )
  - alert: InternalApiServiceRailsRequestErrorSLOViolationRegional
    for: 2m
    annotations:
      title: The rails_request SLI of the internal-api service in region `{{ $labels.region
        }}` has an error rate violating SLO
      description: |
        The number of requests meeting their duration target based on the urgency of the endpoint. By default, a request should take no more than 1s. But this can be adjusted by endpoint.

        Read more about this in the [documentation](https://docs.gitlab.com/ee/development/application_slis/rails_request_apdex.html).

        Note that this alert is specific to the `{{ $labels.region }}` region.

        Currently the error-rate is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: internal-api-main/internal-api-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/internal-api-main/internal-api-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "387946920"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage,region) (
          rate(gitlab_sli_rails_request_error_total{environment="{{ $labels.environment }}",job="gitlab-rails",region="{{ $labels.region }}",stage="{{ $labels.stage }}",type="internal-api"}[5m])
        )
      runbook: docs/internal-api/README.md
    labels:
      aggregation: regional_component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: in_source_metrics
      rules_domain: general
      severity: s3
      sli_type: error
      slo_alert: "yes"
      user_impacting: "yes"
      window: 1h
    expr: |
      (
        (
          gitlab_regional_sli_errors:ratio_1h{component="rails_request",monitor="global",type="internal-api"}
          > (14.4 * 0.001000)
        )
        and
        (
          gitlab_regional_sli_errors:ratio_5m{component="rails_request",monitor="global",type="internal-api"}
          > (14.4 * 0.001000)
        )
      )
      and on(env,environment,tier,type,stage,region,component)
      (
        sum by(env,environment,tier,type,stage,region,component) (gitlab_regional_sli_ops:rate_1h{component="rails_request",monitor="global",type="internal-api"}) >= 1
      )
  - alert: InternalApiServiceRailsRequestErrorSLOViolationRegional
    for: 2m
    annotations:
      title: The rails_request SLI of the internal-api service in region `{{ $labels.region
        }}` has an error rate violating SLO
      description: |
        The number of requests meeting their duration target based on the urgency of the endpoint. By default, a request should take no more than 1s. But this can be adjusted by endpoint.

        Read more about this in the [documentation](https://docs.gitlab.com/ee/development/application_slis/rails_request_apdex.html).

        Note that this alert is specific to the `{{ $labels.region }}` region.

        Currently the error-rate is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: internal-api-main/internal-api-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/internal-api-main/internal-api-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "387946920"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage,region) (
          rate(gitlab_sli_rails_request_error_total{environment="{{ $labels.environment }}",job="gitlab-rails",region="{{ $labels.region }}",stage="{{ $labels.stage }}",type="internal-api"}[5m])
        )
      runbook: docs/internal-api/README.md
    labels:
      aggregation: regional_component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: in_source_metrics
      rules_domain: general
      severity: s3
      sli_type: error
      slo_alert: "yes"
      user_impacting: "yes"
      window: 6h
    expr: |
      (
        (
          gitlab_regional_sli_errors:ratio_6h{component="rails_request",monitor="global",type="internal-api"}
          > (6 * 0.001000)
        )
        and
        (
          gitlab_regional_sli_errors:ratio_30m{component="rails_request",monitor="global",type="internal-api"}
          > (6 * 0.001000)
        )
      )
      and on(env,environment,tier,type,stage,region,component)
      (
        sum by(env,environment,tier,type,stage,region,component) (gitlab_regional_sli_ops:rate_6h{component="rails_request",monitor="global",type="internal-api"}) >= 0.16667
      )
  - alert: InternalApiServiceRailsRequestTrafficCessation
    for: 5m
    annotations:
      title: The rails_request SLI of the internal-api service (`{{ $labels.stage
        }}` stage) has not received any traffic in the past 30m
      description: |
        The number of requests meeting their duration target based on the urgency of the endpoint. By default, a request should take no more than 1s. But this can be adjusted by endpoint.

        Read more about this in the [documentation](https://docs.gitlab.com/ee/development/application_slis/rails_request_apdex.html).

        This alert signifies that the SLI is reporting a cessation of traffic; the signal is present, but is zero.
      grafana_dashboard_id: internal-api-main/internal-api-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/internal-api-main/internal-api-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "538903447"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          sli_aggregations:gitlab_sli_rails_request_total_rate5m{environment="{{ $labels.environment }}",job="gitlab-rails",stage="{{ $labels.stage }}",type="internal-api"}
        )
      runbook: docs/internal-api/README.md
    labels:
      aggregation: component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: in_source_metrics
      rules_domain: general
      severity: s3
      sli_type: ops
      slo_alert: "no"
      user_impacting: "yes"
    expr: |
      gitlab_component_ops:rate_30m{component="rails_request",monitor="global",stage="main",type="internal-api"} == 0
      and
      gitlab_component_ops:rate_30m{component="rails_request",monitor="global",stage="main",type="internal-api"} offset 1h >= 0.16666666666666666
  - alert: InternalApiServiceRailsRequestTrafficAbsent
    for: 30m
    annotations:
      title: The rails_request SLI of the internal-api service (`{{ $labels.stage
        }}` stage) has not reported any traffic in the past 30m
      description: |
        The number of requests meeting their duration target based on the urgency of the endpoint. By default, a request should take no more than 1s. But this can be adjusted by endpoint.

        Read more about this in the [documentation](https://docs.gitlab.com/ee/development/application_slis/rails_request_apdex.html).

        This alert signifies that the SLI was previously reporting traffic, but is no longer - the signal is absent.

        This could be caused by a change to the metrics used in the SLI, or by the service not receiving traffic.
      grafana_dashboard_id: internal-api-main/internal-api-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/internal-api-main/internal-api-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "538903447"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          sli_aggregations:gitlab_sli_rails_request_total_rate5m{environment="{{ $labels.environment }}",job="gitlab-rails",stage="{{ $labels.stage }}",type="internal-api"}
        )
      runbook: docs/internal-api/README.md
    labels:
      aggregation: component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: in_source_metrics
      rules_domain: general
      severity: s3
      sli_type: ops
      slo_alert: "no"
      user_impacting: "yes"
    expr: |
      gitlab_component_ops:rate_5m{component="rails_request",monitor="global",stage="main",type="internal-api"} offset 1h
      unless
      gitlab_component_ops:rate_5m{component="rails_request",monitor="global",stage="main",type="internal-api"}
  - alert: InternalApiServiceRailsRequestTrafficCessationRegional
    for: 5m
    annotations:
      title: The rails_request SLI of the internal-api service in region `{{ $labels.region
        }}` has not received any traffic in the past 30m
      description: |
        The number of requests meeting their duration target based on the urgency of the endpoint. By default, a request should take no more than 1s. But this can be adjusted by endpoint.

        Read more about this in the [documentation](https://docs.gitlab.com/ee/development/application_slis/rails_request_apdex.html).

        This alert signifies that the SLI is reporting a cessation of traffic; the signal is present, but is zero.

        Note that this alert is specific to the `{{ $labels.region }}` region.
      grafana_dashboard_id: internal-api-main/internal-api-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/internal-api-main/internal-api-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "538903447"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage,region) (
          rate(gitlab_sli_rails_request_total{environment="{{ $labels.environment }}",job="gitlab-rails",region="{{ $labels.region }}",stage="{{ $labels.stage }}",type="internal-api"}[5m])
        )
      runbook: docs/internal-api/README.md
    labels:
      aggregation: regional_component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: in_source_metrics
      rules_domain: general
      severity: s3
      sli_type: ops
      slo_alert: "no"
      user_impacting: "yes"
    expr: |
      gitlab_regional_sli_ops:rate_30m{component="rails_request",monitor="global",stage="main",type="internal-api"} == 0
      and
      gitlab_regional_sli_ops:rate_30m{component="rails_request",monitor="global",stage="main",type="internal-api"} offset 1h >= 0.16666666666666666
  - alert: InternalApiServiceRailsRequestTrafficAbsentRegional
    for: 30m
    annotations:
      title: The rails_request SLI of the internal-api service in region `{{ $labels.region
        }}` has not reported any traffic in the past 30m
      description: |
        The number of requests meeting their duration target based on the urgency of the endpoint. By default, a request should take no more than 1s. But this can be adjusted by endpoint.

        Read more about this in the [documentation](https://docs.gitlab.com/ee/development/application_slis/rails_request_apdex.html).

        This alert signifies that the SLI was previously reporting traffic, but is no longer - the signal is absent.

        This could be caused by a change to the metrics used in the SLI, or by the service not receiving traffic.

        Note that this alert is specific to the `{{ $labels.region }}` region.
      grafana_dashboard_id: internal-api-main/internal-api-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/internal-api-main/internal-api-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "538903447"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage,region) (
          rate(gitlab_sli_rails_request_total{environment="{{ $labels.environment }}",job="gitlab-rails",region="{{ $labels.region }}",stage="{{ $labels.stage }}",type="internal-api"}[5m])
        )
      runbook: docs/internal-api/README.md
    labels:
      aggregation: regional_component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: in_source_metrics
      rules_domain: general
      severity: s3
      sli_type: ops
      slo_alert: "no"
      user_impacting: "yes"
    expr: |
      gitlab_regional_sli_ops:rate_5m{component="rails_request",monitor="global",stage="main",type="internal-api"} offset 1h
      unless
      gitlab_regional_sli_ops:rate_5m{component="rails_request",monitor="global",stage="main",type="internal-api"}
  - alert: InternalApiServiceWorkhorseApdexSLOViolation
    for: 2m
    annotations:
      title: The workhorse SLI of the internal-api service (`{{ $labels.stage }}`
        stage) has an apdex violating SLO
      description: |
        Aggregation of most web requests that pass through workhorse, monitored via the HTTP interface. Excludes health, readiness and liveness requests. Some known slow requests, such as HTTP uploads, are excluded from the apdex score.

        Currently the apdex value is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: internal-api-main/internal-api-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/internal-api-main/internal-api-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "2586818196"
      grafana_variables: environment,stage
      promql_template_1: |
        histogram_quantile(
          0.950000,
          sum by (env,environment,tier,stage,le) (
            rate(gitlab_workhorse_http_request_duration_seconds_bucket{job=~"gitlab-workhorse-api|gitlab-workhorse", type="internal-api", route!="^/-/health$", route!="^/-/(readiness|liveness)$", environment="{{ $labels.environment }}",stage="{{ $labels.stage }}"}[5m])
          )
        )
      runbook: docs/internal-api/README.md
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      rules_domain: general
      severity: s3
      sli_type: apdex
      slo_alert: "yes"
      user_impacting: "yes"
      window: 1h
    expr: |
      (
        (
          gitlab_component_apdex:ratio_1h{component="workhorse",monitor="global",type="internal-api"}
          < (1 - 14.4 * 0.005000)
        )
        and
        (
          gitlab_component_apdex:ratio_5m{component="workhorse",monitor="global",type="internal-api"}
          < (1 - 14.4 * 0.005000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_1h{component="workhorse",monitor="global",type="internal-api"}) >= 1
      )
  - alert: InternalApiServiceWorkhorseApdexSLOViolation
    for: 2m
    annotations:
      title: The workhorse SLI of the internal-api service (`{{ $labels.stage }}`
        stage) has an apdex violating SLO
      description: |
        Aggregation of most web requests that pass through workhorse, monitored via the HTTP interface. Excludes health, readiness and liveness requests. Some known slow requests, such as HTTP uploads, are excluded from the apdex score.

        Currently the apdex value is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: internal-api-main/internal-api-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/internal-api-main/internal-api-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "2586818196"
      grafana_variables: environment,stage
      promql_template_1: |
        histogram_quantile(
          0.950000,
          sum by (env,environment,tier,stage,le) (
            rate(gitlab_workhorse_http_request_duration_seconds_bucket{job=~"gitlab-workhorse-api|gitlab-workhorse", type="internal-api", route!="^/-/health$", route!="^/-/(readiness|liveness)$", environment="{{ $labels.environment }}",stage="{{ $labels.stage }}"}[5m])
          )
        )
      runbook: docs/internal-api/README.md
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      rules_domain: general
      severity: s3
      sli_type: apdex
      slo_alert: "yes"
      user_impacting: "yes"
      window: 6h
    expr: |
      (
        (
          gitlab_component_apdex:ratio_6h{component="workhorse",monitor="global",type="internal-api"}
          < (1 - 6 * 0.005000)
        )
        and
        (
          gitlab_component_apdex:ratio_30m{component="workhorse",monitor="global",type="internal-api"}
          < (1 - 6 * 0.005000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_6h{component="workhorse",monitor="global",type="internal-api"}) >= 0.16667
      )
  - alert: InternalApiServiceWorkhorseApdexSLOViolationRegional
    for: 2m
    annotations:
      title: The workhorse SLI of the internal-api service in region `{{ $labels.region
        }}` has an apdex violating SLO
      description: |
        Aggregation of most web requests that pass through workhorse, monitored via the HTTP interface. Excludes health, readiness and liveness requests. Some known slow requests, such as HTTP uploads, are excluded from the apdex score.

        Note that this alert is specific to the `{{ $labels.region }}` region.

        Currently the apdex value is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: internal-api-main/internal-api-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/internal-api-main/internal-api-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "2586818196"
      grafana_variables: environment,stage
      promql_template_1: |
        histogram_quantile(
          0.950000,
          sum by (env,environment,tier,stage,region,le) (
            rate(gitlab_workhorse_http_request_duration_seconds_bucket{job=~"gitlab-workhorse-api|gitlab-workhorse", type="internal-api", route!="^/-/health$", route!="^/-/(readiness|liveness)$", environment="{{ $labels.environment }}",region="{{ $labels.region }}",stage="{{ $labels.stage }}"}[5m])
          )
        )
      runbook: docs/internal-api/README.md
    labels:
      aggregation: regional_component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      rules_domain: general
      severity: s3
      sli_type: apdex
      slo_alert: "yes"
      user_impacting: "yes"
      window: 1h
    expr: |
      (
        (
          gitlab_regional_sli_apdex:ratio_1h{component="workhorse",monitor="global",type="internal-api"}
          < (1 - 14.4 * 0.005000)
        )
        and
        (
          gitlab_regional_sli_apdex:ratio_5m{component="workhorse",monitor="global",type="internal-api"}
          < (1 - 14.4 * 0.005000)
        )
      )
      and on(env,environment,tier,type,stage,region,component)
      (
        sum by(env,environment,tier,type,stage,region,component) (gitlab_regional_sli_ops:rate_1h{component="workhorse",monitor="global",type="internal-api"}) >= 1
      )
  - alert: InternalApiServiceWorkhorseApdexSLOViolationRegional
    for: 2m
    annotations:
      title: The workhorse SLI of the internal-api service in region `{{ $labels.region
        }}` has an apdex violating SLO
      description: |
        Aggregation of most web requests that pass through workhorse, monitored via the HTTP interface. Excludes health, readiness and liveness requests. Some known slow requests, such as HTTP uploads, are excluded from the apdex score.

        Note that this alert is specific to the `{{ $labels.region }}` region.

        Currently the apdex value is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: internal-api-main/internal-api-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/internal-api-main/internal-api-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "2586818196"
      grafana_variables: environment,stage
      promql_template_1: |
        histogram_quantile(
          0.950000,
          sum by (env,environment,tier,stage,region,le) (
            rate(gitlab_workhorse_http_request_duration_seconds_bucket{job=~"gitlab-workhorse-api|gitlab-workhorse", type="internal-api", route!="^/-/health$", route!="^/-/(readiness|liveness)$", environment="{{ $labels.environment }}",region="{{ $labels.region }}",stage="{{ $labels.stage }}"}[5m])
          )
        )
      runbook: docs/internal-api/README.md
    labels:
      aggregation: regional_component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      rules_domain: general
      severity: s3
      sli_type: apdex
      slo_alert: "yes"
      user_impacting: "yes"
      window: 6h
    expr: |
      (
        (
          gitlab_regional_sli_apdex:ratio_6h{component="workhorse",monitor="global",type="internal-api"}
          < (1 - 6 * 0.005000)
        )
        and
        (
          gitlab_regional_sli_apdex:ratio_30m{component="workhorse",monitor="global",type="internal-api"}
          < (1 - 6 * 0.005000)
        )
      )
      and on(env,environment,tier,type,stage,region,component)
      (
        sum by(env,environment,tier,type,stage,region,component) (gitlab_regional_sli_ops:rate_6h{component="workhorse",monitor="global",type="internal-api"}) >= 0.16667
      )
  - alert: InternalApiServiceWorkhorseErrorSLOViolation
    for: 2m
    annotations:
      title: The workhorse SLI of the internal-api service (`{{ $labels.stage }}`
        stage) has an error rate violating SLO
      description: |
        Aggregation of most web requests that pass through workhorse, monitored via the HTTP interface. Excludes health, readiness and liveness requests. Some known slow requests, such as HTTP uploads, are excluded from the apdex score.

        Currently the error-rate is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: internal-api-main/internal-api-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/internal-api-main/internal-api-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "2047984749"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          rate(gitlab_workhorse_http_requests_total{job=~"gitlab-workhorse-api|gitlab-workhorse", type="internal-api", code=~"^5.*", route!="^/-/health$", route!="^/-/(readiness|liveness)$", environment="{{ $labels.environment }}",stage="{{ $labels.stage }}"}[5m])
        )
      runbook: docs/internal-api/README.md
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      rules_domain: general
      severity: s3
      sli_type: error
      slo_alert: "yes"
      user_impacting: "yes"
      window: 1h
    expr: |
      (
        (
          gitlab_component_errors:ratio_1h{component="workhorse",monitor="global",type="internal-api"}
          > (14.4 * 0.001000)
        )
        and
        (
          gitlab_component_errors:ratio_5m{component="workhorse",monitor="global",type="internal-api"}
          > (14.4 * 0.001000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_1h{component="workhorse",monitor="global",type="internal-api"}) >= 1
      )
  - alert: InternalApiServiceWorkhorseErrorSLOViolation
    for: 2m
    annotations:
      title: The workhorse SLI of the internal-api service (`{{ $labels.stage }}`
        stage) has an error rate violating SLO
      description: |
        Aggregation of most web requests that pass through workhorse, monitored via the HTTP interface. Excludes health, readiness and liveness requests. Some known slow requests, such as HTTP uploads, are excluded from the apdex score.

        Currently the error-rate is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: internal-api-main/internal-api-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/internal-api-main/internal-api-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "2047984749"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          rate(gitlab_workhorse_http_requests_total{job=~"gitlab-workhorse-api|gitlab-workhorse", type="internal-api", code=~"^5.*", route!="^/-/health$", route!="^/-/(readiness|liveness)$", environment="{{ $labels.environment }}",stage="{{ $labels.stage }}"}[5m])
        )
      runbook: docs/internal-api/README.md
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      rules_domain: general
      severity: s3
      sli_type: error
      slo_alert: "yes"
      user_impacting: "yes"
      window: 6h
    expr: |
      (
        (
          gitlab_component_errors:ratio_6h{component="workhorse",monitor="global",type="internal-api"}
          > (6 * 0.001000)
        )
        and
        (
          gitlab_component_errors:ratio_30m{component="workhorse",monitor="global",type="internal-api"}
          > (6 * 0.001000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_6h{component="workhorse",monitor="global",type="internal-api"}) >= 0.16667
      )
  - alert: InternalApiServiceWorkhorseErrorSLOViolationRegional
    for: 2m
    annotations:
      title: The workhorse SLI of the internal-api service in region `{{ $labels.region
        }}` has an error rate violating SLO
      description: |
        Aggregation of most web requests that pass through workhorse, monitored via the HTTP interface. Excludes health, readiness and liveness requests. Some known slow requests, such as HTTP uploads, are excluded from the apdex score.

        Note that this alert is specific to the `{{ $labels.region }}` region.

        Currently the error-rate is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: internal-api-main/internal-api-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/internal-api-main/internal-api-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "2047984749"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage,region) (
          rate(gitlab_workhorse_http_requests_total{job=~"gitlab-workhorse-api|gitlab-workhorse", type="internal-api", code=~"^5.*", route!="^/-/health$", route!="^/-/(readiness|liveness)$", environment="{{ $labels.environment }}",region="{{ $labels.region }}",stage="{{ $labels.stage }}"}[5m])
        )
      runbook: docs/internal-api/README.md
    labels:
      aggregation: regional_component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      rules_domain: general
      severity: s3
      sli_type: error
      slo_alert: "yes"
      user_impacting: "yes"
      window: 1h
    expr: |
      (
        (
          gitlab_regional_sli_errors:ratio_1h{component="workhorse",monitor="global",type="internal-api"}
          > (14.4 * 0.001000)
        )
        and
        (
          gitlab_regional_sli_errors:ratio_5m{component="workhorse",monitor="global",type="internal-api"}
          > (14.4 * 0.001000)
        )
      )
      and on(env,environment,tier,type,stage,region,component)
      (
        sum by(env,environment,tier,type,stage,region,component) (gitlab_regional_sli_ops:rate_1h{component="workhorse",monitor="global",type="internal-api"}) >= 1
      )
  - alert: InternalApiServiceWorkhorseErrorSLOViolationRegional
    for: 2m
    annotations:
      title: The workhorse SLI of the internal-api service in region `{{ $labels.region
        }}` has an error rate violating SLO
      description: |
        Aggregation of most web requests that pass through workhorse, monitored via the HTTP interface. Excludes health, readiness and liveness requests. Some known slow requests, such as HTTP uploads, are excluded from the apdex score.

        Note that this alert is specific to the `{{ $labels.region }}` region.

        Currently the error-rate is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: internal-api-main/internal-api-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/internal-api-main/internal-api-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "2047984749"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage,region) (
          rate(gitlab_workhorse_http_requests_total{job=~"gitlab-workhorse-api|gitlab-workhorse", type="internal-api", code=~"^5.*", route!="^/-/health$", route!="^/-/(readiness|liveness)$", environment="{{ $labels.environment }}",region="{{ $labels.region }}",stage="{{ $labels.stage }}"}[5m])
        )
      runbook: docs/internal-api/README.md
    labels:
      aggregation: regional_component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      rules_domain: general
      severity: s3
      sli_type: error
      slo_alert: "yes"
      user_impacting: "yes"
      window: 6h
    expr: |
      (
        (
          gitlab_regional_sli_errors:ratio_6h{component="workhorse",monitor="global",type="internal-api"}
          > (6 * 0.001000)
        )
        and
        (
          gitlab_regional_sli_errors:ratio_30m{component="workhorse",monitor="global",type="internal-api"}
          > (6 * 0.001000)
        )
      )
      and on(env,environment,tier,type,stage,region,component)
      (
        sum by(env,environment,tier,type,stage,region,component) (gitlab_regional_sli_ops:rate_6h{component="workhorse",monitor="global",type="internal-api"}) >= 0.16667
      )
  - alert: InternalApiServiceWorkhorseTrafficCessation
    for: 5m
    annotations:
      title: The workhorse SLI of the internal-api service (`{{ $labels.stage }}`
        stage) has not received any traffic in the past 30m
      description: |
        Aggregation of most web requests that pass through workhorse, monitored via the HTTP interface. Excludes health, readiness and liveness requests. Some known slow requests, such as HTTP uploads, are excluded from the apdex score.

        This alert signifies that the SLI is reporting a cessation of traffic; the signal is present, but is zero.
      grafana_dashboard_id: internal-api-main/internal-api-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/internal-api-main/internal-api-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "831392945"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          rate(gitlab_workhorse_http_requests_total{job=~"gitlab-workhorse-api|gitlab-workhorse", type="internal-api", environment="{{ $labels.environment }}",stage="{{ $labels.stage }}"}[5m])
        )
      runbook: docs/internal-api/README.md
    labels:
      aggregation: component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: not_owned
      rules_domain: general
      severity: s3
      sli_type: ops
      slo_alert: "no"
      user_impacting: "yes"
    expr: |
      gitlab_component_ops:rate_30m{component="workhorse",monitor="global",stage="main",type="internal-api"} == 0
      and
      gitlab_component_ops:rate_30m{component="workhorse",monitor="global",stage="main",type="internal-api"} offset 1h >= 0.16666666666666666
  - alert: InternalApiServiceWorkhorseTrafficAbsent
    for: 30m
    annotations:
      title: The workhorse SLI of the internal-api service (`{{ $labels.stage }}`
        stage) has not reported any traffic in the past 30m
      description: |
        Aggregation of most web requests that pass through workhorse, monitored via the HTTP interface. Excludes health, readiness and liveness requests. Some known slow requests, such as HTTP uploads, are excluded from the apdex score.

        This alert signifies that the SLI was previously reporting traffic, but is no longer - the signal is absent.

        This could be caused by a change to the metrics used in the SLI, or by the service not receiving traffic.
      grafana_dashboard_id: internal-api-main/internal-api-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/internal-api-main/internal-api-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "831392945"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          rate(gitlab_workhorse_http_requests_total{job=~"gitlab-workhorse-api|gitlab-workhorse", type="internal-api", environment="{{ $labels.environment }}",stage="{{ $labels.stage }}"}[5m])
        )
      runbook: docs/internal-api/README.md
    labels:
      aggregation: component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: not_owned
      rules_domain: general
      severity: s3
      sli_type: ops
      slo_alert: "no"
      user_impacting: "yes"
    expr: |
      gitlab_component_ops:rate_5m{component="workhorse",monitor="global",stage="main",type="internal-api"} offset 1h
      unless
      gitlab_component_ops:rate_5m{component="workhorse",monitor="global",stage="main",type="internal-api"}
  - alert: InternalApiServiceWorkhorseTrafficCessationRegional
    for: 5m
    annotations:
      title: The workhorse SLI of the internal-api service in region `{{ $labels.region
        }}` has not received any traffic in the past 30m
      description: |
        Aggregation of most web requests that pass through workhorse, monitored via the HTTP interface. Excludes health, readiness and liveness requests. Some known slow requests, such as HTTP uploads, are excluded from the apdex score.

        This alert signifies that the SLI is reporting a cessation of traffic; the signal is present, but is zero.

        Note that this alert is specific to the `{{ $labels.region }}` region.
      grafana_dashboard_id: internal-api-main/internal-api-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/internal-api-main/internal-api-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "831392945"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage,region) (
          rate(gitlab_workhorse_http_requests_total{job=~"gitlab-workhorse-api|gitlab-workhorse", type="internal-api", environment="{{ $labels.environment }}",region="{{ $labels.region }}",stage="{{ $labels.stage }}"}[5m])
        )
      runbook: docs/internal-api/README.md
    labels:
      aggregation: regional_component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: not_owned
      rules_domain: general
      severity: s3
      sli_type: ops
      slo_alert: "no"
      user_impacting: "yes"
    expr: |
      gitlab_regional_sli_ops:rate_30m{component="workhorse",monitor="global",stage="main",type="internal-api"} == 0
      and
      gitlab_regional_sli_ops:rate_30m{component="workhorse",monitor="global",stage="main",type="internal-api"} offset 1h >= 0.16666666666666666
  - alert: InternalApiServiceWorkhorseTrafficAbsentRegional
    for: 30m
    annotations:
      title: The workhorse SLI of the internal-api service in region `{{ $labels.region
        }}` has not reported any traffic in the past 30m
      description: |
        Aggregation of most web requests that pass through workhorse, monitored via the HTTP interface. Excludes health, readiness and liveness requests. Some known slow requests, such as HTTP uploads, are excluded from the apdex score.

        This alert signifies that the SLI was previously reporting traffic, but is no longer - the signal is absent.

        This could be caused by a change to the metrics used in the SLI, or by the service not receiving traffic.

        Note that this alert is specific to the `{{ $labels.region }}` region.
      grafana_dashboard_id: internal-api-main/internal-api-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/internal-api-main/internal-api-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "831392945"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage,region) (
          rate(gitlab_workhorse_http_requests_total{job=~"gitlab-workhorse-api|gitlab-workhorse", type="internal-api", environment="{{ $labels.environment }}",region="{{ $labels.region }}",stage="{{ $labels.stage }}"}[5m])
        )
      runbook: docs/internal-api/README.md
    labels:
      aggregation: regional_component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: not_owned
      rules_domain: general
      severity: s3
      sli_type: ops
      slo_alert: "no"
      user_impacting: "yes"
    expr: |
      gitlab_regional_sli_ops:rate_5m{component="workhorse",monitor="global",stage="main",type="internal-api"} offset 1h
      unless
      gitlab_regional_sli_ops:rate_5m{component="workhorse",monitor="global",stage="main",type="internal-api"}
